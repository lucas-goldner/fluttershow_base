name: flutter_airpods_example
description: Demonstrates how to use the flutter_airpods plugin.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=3.0.3 <4.0.0"

dependencies:
  cupertino_icons: 1.0.5
  flutter:
    sdk: flutter

  fluttershow_base:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
